# ============================================================
# apps/app3r — Example environment variables (MOCKUP)
# Copy to .env.local and adjust. Never commit real domains here.
# ============================================================

# Cross-app URL stubs (resolved via lib/config/urls.ts). Localhost only in mockup.
NEXT_PUBLIC_WEEEU_URL=http://localhost:3002
NEXT_PUBLIC_WEEER_URL=http://localhost:3001
NEXT_PUBLIC_WEEET_URL=http://localhost:3003

# Dev-only Screen ID badge + Mock Role switcher (must be unset in production).
# NEXT_PUBLIC_DEV_NAV=1
