# ============================================================
# apps/app3r — Example environment variables (MOCKUP)
# Copy to .env.local and adjust. Never commit real domains here.
# ============================================================

# Cross-app URL stubs (RoleAwareCTA / cross-app links). Localhost only in mockup.
NEXT_PUBLIC_WEEEU_URL=http://localhost:3002
NEXT_PUBLIC_WEEER_URL=http://localhost:3001
