# ─── DAL Feature Flags (สวิตช์เปิด/ปิด) — D84 Adapter Pattern ───────────────
# คัดลอกไฟล์นี้เป็น .env.local และตั้งค่าตามต้องการ
#
# Phase D-1: ทุก flag = false → ใช้ localStorageAdapter (Phase C mock)
# Phase D-2: ตั้ง true ทีละ module เพื่อ migrate ไป real API

NEXT_PUBLIC_USE_API_AUTH=false
NEXT_PUBLIC_USE_API_PROGRESS=false
NEXT_PUBLIC_USE_API_APPLIANCES=false
NEXT_PUBLIC_USE_API_REPAIR=false
